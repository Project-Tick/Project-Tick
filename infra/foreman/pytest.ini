[pytest]
pythonpath = .
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
markers =
	no_default_ci_rule: use the real distributed workflow registry instead of the permissive legacy CI test rule