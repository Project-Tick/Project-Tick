@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

set(NBT_USE_ZLIB @NBT_USE_ZLIB@)
if(NBT_USE_ZLIB)
    find_dependency(neozip)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/nbt++-targets.cmake")

check_required_components(nbt++)
